"use client";

import { Card as BaseCard, CardContent as BaseCardContent, CardHeader as BaseCardHeader, CardTitle as BaseCardTitle } from "@/components/ui/card";

export const UICardContainer = BaseCard;
export const UICardContent = BaseCardContent;
export const UICardHeader = BaseCardHeader;
export const UICardTitle = BaseCardTitle;


